import { defineConfig } from 'astro/config';

// Project site under the EraChanZZZ account → served at
// https://erachanzzz.github.io/VladimirSachkov/
// If you later move to a custom domain or user site, set base back to '/'.
export default defineConfig({
  site: 'https://erachanzzz.github.io',
  base: '/VladimirSachkov',
});
